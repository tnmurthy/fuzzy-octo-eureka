# Runtime-generated telemetry (written by telemetry-daemon.ps1 every cycle)
telemetry.json
history.json

# Legacy output format, in case it's still lying around from before the refactor
telemetry.js

# Node
node_modules/
npm-debug.log*

# OS noise
.DS_Store
Thumbs.db

# Never commit secrets
*.env
apis.txt
